# Local state / plans — this directory stores config only, never state
*.tfstate
*.tfstate.*
*.tfplan
.terraform/
.terraform.lock.hcl
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
# Local filled-in values (never commit resolved secrets)
*.local.tfvars
